# RISC-V Processor Implementation

All the testcases are in the folder "Testcases". To run, execute the following commands in Git Bash:

```[bash]
   chmod +x script.sh 
   ./script.sh 

   # You can execute the following command later:
   # bash script.sh
```
